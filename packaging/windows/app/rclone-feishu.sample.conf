[Feishu]
type = feishu
command = lark-cli
docs_as_url = true

# Optional: set this to mount a specific Feishu Drive folder as the remote root.
# Leave empty or omit it to use the current user's Drive root.
# root_folder_token =
